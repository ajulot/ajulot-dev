# The main pipeline for ajulot_first_pipeline_bundle

resources:
  pipelines:
    ajulot_first_pipeline_bundle_etl:
      name: ajulot_first_pipeline_bundle_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/ajulot_first_pipeline_bundle_etl"

      libraries:
        - glob:
            include: ../src/ajulot_first_pipeline_bundle_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
