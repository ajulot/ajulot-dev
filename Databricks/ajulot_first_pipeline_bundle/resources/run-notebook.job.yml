resources:
  jobs:
    run_notebook:
      name: run-notebook
      queue:
        enabled: true
      tasks:
        - task_key: my-notebook-task
          notebook_task:
            notebook_path: /Workspace/Users/ajulot@gmail.com/ajulot-dev/Databricks/ajulot_first_pipeline_bundle/src/ajulot_first_pipeline_bundle_etl/helloworld
            
      # tasks:
      #   - task_key: notebook_task
      #     notebook_task:
      #       notebook_path: ../path/to/notebook.ipynb
      # email_notifications:
      #   on_failure:
      #     - ajulot@gmail.com
      # trigger:
      #   # Run this job every day, exactly one day from the last run; see https://docs.databricks.com/api/workspace/jobs/create#trigger
      #   periodic:
      #     interval: 1
      #     unit: DAYS
